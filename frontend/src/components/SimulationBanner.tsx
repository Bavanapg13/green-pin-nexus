export default function SimulationBanner() {
  return (
    <div className="absolute bottom-0 left-0 right-0 bg-primary/20 border-t border-primary/30 text-primary py-2 text-center text-xs font-bold tracking-widest z-50 backdrop-blur-sm pointer-events-none">
      SIMULATION ENVIRONMENT ACTIVE
    </div>
  );
}
